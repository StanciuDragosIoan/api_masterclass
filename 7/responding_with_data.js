/*
    //respond with text data
    res.write('Hello');

    //there are request headers and response headers

    request headers (we access them through request object)
    ! note if server returning only 1 thing, it can be put in res.end(); //no need for res.write()
*/
